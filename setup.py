from setuptools import setup, find_packages

setup(
   name='pepflow',
   version='1.0',
   description='A module for all-atom peptide conformation sampling',
   author='Osama Abdin',
   author_email='osama.abdin@kimlab.org',
   packages=  find_packages(),
   include_package_data=True,

)
